version: '3'

tasks:
  start_userinfo:
    desc: "Start the local UserInfo service"
    cmds:
      - echo "Starting local UserInfo service..."
      - go run cmd/main.go --config=./.env
    ignore_error: true
    silent: false
